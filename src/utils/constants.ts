export enum PossibleRelatives {
  FATHER = "Father",
  MOTHER = "Mother",
  BROTHER = "Brother",
  SISTER = "Sister",
}

export enum ErrorMessages {
  INVALID_DATE_FORMAT = "Invalid date format",
}

export enum JSONComparisonMessages {
  IDENTICAL = "The JSON files are identical.",
  DIFFERENT = "The JSON files are different.",
}

export const ExpectedDateFormat = "MM/DD/YYYY";

export enum Paths {
    RESULT_OUTPUT = "output.json",
    DESIRED_RESULT = "src/utils/expected-output.json",
    INPUT = "input.csv"
}

